1. 
python3 -m venv restframework
source restframework/bin/activate

2.
python -m pip install django
@instal  psycopg2-binary-2.9.10 for postgresql connection
@instal ( pip install python-decouple) for environement purposes


dont forget to change the engine in the settings.py file

3. implement jwt token for authentication

4 if you got an error like this:

"django.db.migrations.exceptions.InconsistentMigrationHistory: Migration admin.0001_initial is applied before its dependency users.0001_initial on database 'default'."

comment out the admin in the installed apps in the settings.py file